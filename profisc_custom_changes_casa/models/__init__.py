# Backend handling of `is_draft` (field + POS data loading + fiscalization skip)
# is provided by the base `profisc` module, so no pos.order override is needed
# here. Re-enable the import below only if that turns out not to be the case.
# from . import pos_order
